# ⚠️  DEPRECATED — canonical source of dependencies is pyproject.toml
# This file is kept only for backward compatibility with legacy tooling.
# To install:  pip install -e ".[dev]"        (core + dev tools)
#              pip install -e ".[all]"        (everything)
#
# Regenerate from pyproject.toml if needed:
#   pip-compile pyproject.toml -o requirements.txt

# ─── Core (pyproject.toml → [project].dependencies) ─────────────────────────
numpy>=1.26.0
psutil>=5.9.0
jsonlines>=4.0.0
tqdm>=4.66.0

# ─── Dev (pyproject.toml → [project.optional-dependencies].dev) ──────────────
pytest>=8.0
pytest-cov>=5.0
ruff>=0.4.0
mypy>=1.10

# ─── NLP (pyproject.toml → [project.optional-dependencies].nlp) ─────────────
pymorphy3>=1.0.0
razdel>=0.5.0
nltk>=3.8.0
navec>=0.10.0
sentence-transformers>=2.7.0

# ─── Vision (pyproject.toml → [project.optional-dependencies].vision) ────────
open-clip-torch>=2.24.0
pillow>=10.0.0

# ─── Audio (pyproject.toml → [project.optional-dependencies].audio) ──────────
openai-whisper>=20231117

# ─── Docs (pyproject.toml → [project.optional-dependencies].docs) ────────────
pymupdf>=1.24.0
python-docx>=1.1.0
